---
category: Site
---
La carte de recette, partagée par la home et le hub : photo 4/5 (ou nappe vichy avec l'emblème quand il n'y a pas de photo), titre en Fraunces, catégorie et temps total en petites capitales bois. Prend un objet `recipe` — les champs utiles au rendu sont `slug`, `title`, `cover` (URL ou null), `category`, `minutes`, `prepMinutes`, `seasons`. S'emploie dans une grille `sm:grid-cols-2 lg:grid-cols-3`.
