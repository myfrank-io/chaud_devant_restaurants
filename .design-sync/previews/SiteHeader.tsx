import { SiteHeader } from 'chaud-devant'

/** L'en-tête du site : nappe, logotype rouge, navigation Recettes / Le concept. */
export const EnTete = () => (
  <div className="bg-creme">
    <SiteHeader />
  </div>
)
