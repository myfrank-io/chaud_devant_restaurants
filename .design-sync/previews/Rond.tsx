import { Rond } from 'chaud-devant'

/** Le disque plein, cocotte évidée : photos de profil et favicons. */
export const Fonte = () => (
  <div className="papier p-10">
    <Rond className="mx-auto w-40 text-fonte" />
  </div>
)

/** La même pastille à l'encre rouge de la maison. */
export const Rouge = () => (
  <div className="papier p-10">
    <Rond className="mx-auto w-40 text-rouge" />
  </div>
)
